# Assumptions

Cross-source assumption index. Each entry is flagged distinctly from confirmed facts (no direct citation in the source). Each `## From <source>` section is regenerated by its owning `/tc:learn-from-*` command; sections from other commands are preserved across re-runs.

## From documents

_No assumptions extracted from documents._
