# 2026-06-14

## 2026-06-14T00:14:34Z

Built functional + regression API coverage in the Playwright framework (tests/api/, tests/regression/, shared tests/api/_helpers.ts; api project, --workers=1 because the suite shares one backend DB). 13 functional tests pass (appointment happy-path/relationship/filters/status; patient CRUD, duplicate-email and invalid-email validation, referential-integrity on patient delete). 5 regression tests red by design: BUG-01 past-date accepted, BUG-02 inactive doctor bookable, BUG-03 double-booking, BUG-06 patient search unimplemented (GET /patients ignores search), BUG-07 future DOB accepted. BUG-07 was missed in manual probing because a missing required field (insurance_provider, which is a required enum) returned 422 and masked the real behavior - the functional test caught it. Added shared-state isolation note to test plan section 10. Additional implemented-vs-spec gaps noted: no date filter on GET /appointments (REQ-048), doctors filter only by department_id (REQ-015), insurance_provider required enum not optional (REQ-021).

## 2026-06-14T00:54:04Z

Extended coverage down the gap list. Built requirement->test coverage map (testplan/coverage-map.md): 21 automated-pass, 8 automated-red, 7 partial, 5 implicit, 1 N/A, 9 uncovered, 3 out-of-scope across 54 REQs. Added Departments functional spec (list, create-unique), appointment enum validation (invalid appointment_type/priority/status rejected -> REQ-031/032), and UI validation spec (patient+doctor duplicate-email surfaced in error-message, appointment symptoms<10 client error, native required guard). Two new defects found and turned red: BUG-08 duplicate department name accepted (REQ-008), BUG-09 invalid phone format accepted (REQ-020). Reclassified BUG-04 as not-a-defect: the appointment form correctly blocks incomplete submit via native HTML5 required validation (browser bubble, no in-app message) - confirmed by a passing UI test. REQ-030 inactive-patient is N/A (patients have no active field). Suite now: 29 functional pass + 7 regression red.

